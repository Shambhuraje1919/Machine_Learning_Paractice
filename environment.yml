name: base
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.13
  
  # --- Core Data & Machine Learning ---
  - pandas
  - numpy
  - scikit-learn
  - scipy
  - matplotlib
  
  # --- Web Scraping & Networking ---
  - requests
  - beautifulsoup4
  - scrapy
  - w3lib
  
  # --- Windows-specific dependencies ---
  - win_inet_pton=1.1.0  # [win]
  - winpty=0.4.3  # [win]
  - xlwings=0.32.1  # [win]
  
  # --- Add any other SPECIFIC libraries you actually imported in your code ---
